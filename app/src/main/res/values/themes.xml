<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="LightTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryVariant">@color/lightModeBackGround</item>
        <item name="colorOnPrimary">@color/lightModeBackGroundBar</item>
        <!-- Secondary brand color. -->

        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/DarkModeBackGroundBar</item>
        <item name="android:backgroundTint">@color/lightModeBackGround</item>

        <item name="buttonStyle">@style/ButtonStyleWhite</item> <!-- Apply the white button style -->

        <!-- Customize your theme here. -->
    </style>
</resources>